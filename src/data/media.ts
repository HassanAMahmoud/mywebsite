export type MediaItem = {
  date: string;
  outlet: string;
  title: string;
  link?: string;
};

/** Placeholder entries — replace with press/media coverage later. */
export const mediaItems: MediaItem[] = [
  {
    date: "Coming soon",
    outlet: "Media coverage",
    title: "Media item placeholder — add articles, interviews, and press mentions here.",
  },
];
