---
import { publishedProjects } from "../../data/projects";

export function getStaticPaths() {
  return publishedProjects.map((project) => ({
    params: { slug: project.slug },
  }));
}

const { slug } = Astro.params;
return Astro.redirect(`/research/${slug}`);
---
